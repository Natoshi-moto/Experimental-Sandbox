You are Fable. status_authority: NONE

Resume SBX-EXP-ONE-MACHINE-LEDGER as a **toy test net** only.
Zone: Experimental Sandbox. Lab (~/Lab/) is read-only unless operator says ASK LAB.

Read first (in order):
  ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER/FABLE_HANDOFF.md
  ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER/FABLE_DICTIONARY.md
  ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER/FABLE_MAX_PUSH.md
  ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER/PAPER.md
  ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER/TOY_TESTNET_HANDOFF.md

Then:
  cd ~/Projects/Experimental-Sandbox/experiments/SBX-EXP-ONE-MACHINE-LEDGER
  npm run verify
Do not start coding until verify is green (or you document a real fail).

## Operator intent (plain English → your framing)

Make this toy **more fail-closed / more “safe” as a local referee** than uncorroborated balance theater — and stricter than Bitcoin-*cosplay* on a laptop.

**Do NOT claim** OML replaces Bitcoin, is safer *as open-network money*, is mainnet, is multi-party BFT, or carries real value.
**Do own** the narrow problem so hard that narrative ledgers look like Monopoly paper.

## Job: SAFETY MAX slice (additive)

Push local mechanical safety without cosplay. Prefer small, testable diffs.

### A) Kernel / inspect fail-closed hardening
- On replay: strict exact-key validation for genesis body + allocation rows + blocks (no smuggled fields ignored).
- oml-inspect: always check computed supply == sum(genesis allocations); reject drift even if declared_supply omitted.
- Reject non-array `blocks`, float/unsafe/zero amounts already enforced in paths; close any gaps with tests.
- Keep Node + Python dual in lockstep if you change validation (good fixture must still match pinned anchors).

### B) Adversarial paste corpus expansion
Add more files under fixtures/adversarial/ (each must npm run inspect → INVALID + error_code):
- tip-lie (wrong expected_tip_hash → TIP_MISMATCH)
- unknown-field / smuggled key on genesis or block → BAD_FIELDS (after strict keys)
- wrong-protocol claim as paste shape
- any other high-value fail-closed shapes you find missing vs E01–E14

Keep drop-payload-rider.json VALID with pinned state_root.
Update test/paste-corpus.test.mjs threshold (≥5 → whatever new count is).

### C) Unit tests E15+
Numeric/structural:
- float amount, zero amount, unsafe integer
- unknown field on tx/block/genesis
- genesis supply pin / conservation invariant after multi-hop
Document new ids in PAPER.md §6 Attacks table.

### D) Honest SAFETY doc (required)
Write SAFETY_PROPERTIES.md (or similar) with:
1. Dimensions where this toy is *stricter locally* (dual-impl, poison fail-closed, exact fields, models never define validity, no auto-FINAL, zero real value by design).
2. Explicit non-claims vs Bitcoin (open network, reorg markets, Sybil, key theft, multi-party honesty, etc.).
3. One-sentence punchline from FABLE_DICTIONARY.md elevator.
Language: library systems meeting, not coin marketing.

### E) Verify + handoff
- npm run verify stays PASS (Node tests + fixture + Python dual + toynet poison).
- Update FABLE_HANDOFF.md + Desktop PASTE_ME_TO_FABLE.txt for the *next* seat.
- Lab untouched. No public push unless operator says GO.
- status_authority: NONE on every new packet/receipt/doc.

## Pinned anchors (must not regress)
- genesis_hash: sha256:5fcc89b0a1608a1b6b505b5a6061794899c4c6db80c9f9be31f2c71cf75f8568
- tip_hash:     sha256:1f940c5533579f5de292357d6521635a09e9017ca01fac716ef45138f2acf1df
- state_root:   sha256:e74fb138395a5b96fc83785b36c23cc5b79e88fdccb27fbed25e0d661b3f01b2
- height/supply: 2 / 250 OML_UNIT

## Tone law (hard)
Say: toy test net, append-only validated log, OML_UNIT, state root, local replay.
Don’t say: our Bitcoin, coin, mainnet, currency, investment, “safer than Bitcoin” as money.

## Done when
1) npm run verify PASS
2) ≥2 new adversarial paste files INVALID as expected
3) SAFETY_PROPERTIES.md exists with non-claims
4) Hand off one paragraph: what got safer (local fail-closed), what is still out of scope

Begin.
